let num = [10, 20, 30, 40, 50];

// Loop "for" que itera sobre o array "num"
// "i" é a variável que assume o valor do índice do elemento do array em cada iteração
for (let i = 0; i < num.length; i++) {
  // Imprime o valor do elemento do array que está na posição "i"
  console.log(num[i]);
}